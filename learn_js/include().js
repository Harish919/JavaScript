let msg = "The statue look amazing !!";
console.log(msg.includes("Look"));

let msgs = `The slice() method allows you to extract a portion of a string and returns a new string, without modifying the original string. It takes two parameter
 the starting index and the optional ending index.`;
 console.log(msgs.includes("index1", 10));