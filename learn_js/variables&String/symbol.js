let id1 = Symbol("user");
let id2 = Symbol("user");

console.log(id1 == id2); // false
/* 
Symbol = Unique ID
In JavaScript, Symbol works like that unique ID.
Even if two symbols look same, they are always different. */